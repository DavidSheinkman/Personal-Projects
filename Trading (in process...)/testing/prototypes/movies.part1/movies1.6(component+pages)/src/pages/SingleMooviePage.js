import React from "react";

import ShowInfo from "../components/ShowInfo";

const SingleMooviePage = ({ show, onClose }) => {
  return <ShowInfo show={show} onClose={onClose} />;
};

export default SingleMooviePage;
